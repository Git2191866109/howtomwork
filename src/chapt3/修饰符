private这个大家都知道，就是只有在本类中才能访问。
public正好和private相反，在任何地方都能访问。
protected是在本包内能访问，而在包外只有它的子类能访问。
如图，package1这个包中有一个A类，它有一个成员变量a，用protected访问控制符进行修饰。


也就是说,一个http请求到servle服务器,
HTTP 协议: 请求行 |  消息头  |  请求正文
    请求:
        请求行:
            客户端的请求方式（Method表示请求方法）:eg:GET /form.html HTTP/1.1 (CRLF)
                GET     请求获取Request-URI所标识的资源
                POST    在Request-URI所标识的资源后附加新的数据
                HEAD    请求获取由Request-URI所标识的资源的响应消息报头
                PUT     请求服务器存储一个资源，并用Request-URI作为其标识
                DELETE  请求服务器删除Request-URI所标识的资源
                TRACE   请求服务器回送收到的请求信息，主要用于测试或诊断
                CONNECT 保留将来使用
                OPTIONS 请求查询服务器的性能，或者查询与资源相关的选项和需求
        消息头:
            消息报头包括普通报头、请求报头、响应报头、实体报头。
            每一个报头域都是由名字+“：”+空格+值 组成，消息报头域的名字是大小写无关的。

        换行
        实体内容:
            浏览器端通过http协议发送给服务器的实体数据

    响应:
        状态行:
            例如：  HTTP/1.1  200 OK   （协议的版本号是1.1  响应状态码为200  响应结果为 OK）
        消息报头:
        响应正文:



服务器会解析:http header | 请求的 URI